import React, {useMemo, useState} from 'react';
import {createRoot} from 'react-dom/client';
import './style.css';

const products=[{id:1,name:'Headphone Pro',cat:'Áudio',price:299},{id:2,name:'Teclado Mecânico',cat:'Periféricos',price:249},{id:3,name:'Mouse Gamer',cat:'Periféricos',price:129},{id:4,name:'Monitor 24"',cat:'Monitores',price:799},{id:5,name:'Webcam Full HD',cat:'Acessórios',price:189},{id:6,name:'Hub USB-C',cat:'Acessórios',price:99}];
function App(){const [q,setQ]=useState('');const [cat,setCat]=useState('Todos');const [cart,setCart]=useState(()=>JSON.parse(localStorage.getItem('cart')||'[]'));function add(p){const n=[...cart,p];setCart(n);localStorage.setItem('cart',JSON.stringify(n))}const cats=['Todos',...new Set(products.map(p=>p.cat))];const list=products.filter(p=>(cat==='Todos'||p.cat===cat)&&p.name.toLowerCase().includes(q.toLowerCase()));const total=cart.reduce((a,b)=>a+b.price,0);return <><div className="nav"><strong>DevStore</strong> <span className="badge">{cart.length} itens</span></div><main className="container grid"><section><h1>E-commerce responsivo</h1><p className="muted">Loja com busca, filtros, carrinho e checkout simulado.</p></section><section className="card grid cols" style={{gridTemplateColumns:'2fr 1fr 1fr'}}><input className="input" placeholder="Buscar produto" value={q} onChange={e=>setQ(e.target.value)}/><select value={cat} onChange={e=>setCat(e.target.value)}>{cats.map(c=><option key={c}>{c}</option>)}</select><button className="btn secondary" onClick={()=>{setCart([]);localStorage.removeItem('cart')}}>Limpar carrinho</button></section><section className="grid cols" style={{gridTemplateColumns:'2fr 1fr'}}><div className="grid cols" style={{gridTemplateColumns:'repeat(3,1fr)'}}>{list.map(p=><article className="card" key={p.id}><div style={{height:110,borderRadius:16,background:'linear-gradient(135deg,#e5e7eb,#fff)',marginBottom:12}}></div><span className="badge">{p.cat}</span><h3>{p.name}</h3><p className="stat">R$ {p.price}</p><button className="btn" onClick={()=>add(p)}>Adicionar</button></article>)}</div><aside className="card"><h2>Carrinho</h2>{cart.map((p,i)=><p key={i}>{p.name} — <b>R$ {p.price}</b></p>)}<hr/><h3>Total: R$ {total}</h3><button className="btn" onClick={()=>alert('Checkout simulado finalizado!')}>Finalizar compra</button></aside></section></main></>}
createRoot(document.getElementById('root')).render(<App/>);
